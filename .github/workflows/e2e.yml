name: E2E

on: push

jobs:
  test:
    runs-on: ${{ matrix.os }}
    env:
      MONGODB_URI: ${{ secrets.MONGODB_URI }}
    strategy:
      matrix:
        node-version: [14.15.x]
        os: [ubuntu-latest]
    steps:
    - uses: actions/checkout@v1

    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v1
      with:
        node-version: ${{ matrix.node-version }}

    - name: npm install and test e2e\
      run: |
        npm install
        npm run e2e
