{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/RReporter.sln"
            ],
            "dependsOn": "npm: build",
            "problemMatcher": "$tsc",
            "group": "build",
        }
    ]
}