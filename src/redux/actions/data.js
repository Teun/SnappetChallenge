import {createAction} from 'redux-actions';

export const setByUserId = createAction('SET_BY_USER_ID');
export const setByDomain = createAction('SET_BY_DOMAIN');
export const setFocusedUser = createAction('SET_FOCUSED_USER');
